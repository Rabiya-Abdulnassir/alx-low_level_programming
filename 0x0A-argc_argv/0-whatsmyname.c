#include <stdio.h>
#include <stdlib.h>
/**
* main - fuction
* @argc: number of thing that we enter in comand line
* @argv: string of array
* Return: argument
*/

int main(int argc, char *argv[])
{
printf("argc = %d\n", argc);
printf("%s\n", argv[0]);
return (0);
}
