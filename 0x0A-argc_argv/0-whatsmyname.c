#include <stdio.h>
/**
* main - fuctio
* @argc: number of thing that we enter in comand line
* @argv: string of array
* Return: argument
*/

int main(int argc, char* argv[])
{
printf("%s\n", argv[0]);
return (0);
}
