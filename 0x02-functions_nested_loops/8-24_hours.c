#include "main.h"
/**
 * void - printing mintis of the track
 *
 * Return: no value 
 */
void jack_bauer(void)
{
	int i;
	int j;

	for (i = 0 ; i <= 23 ; i++)
	{
		for (j = 0 ; j <= 60 ; j++)
		{
			_putchar((i / 10) = 0);
			_putchat((i % 10) = 0);
			_putchar(':');
			_putchar((j / 10) = 0);
                        _putchat((j % 10) = 0);
			_putchar('\n');
		}
		 _putchar('\n');
	}
}

