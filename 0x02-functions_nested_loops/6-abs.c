#include "main.h"
/**
 * _abs - print the absolute of integer
 * @n: integer number
 *
 * Return: Always 0
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}

