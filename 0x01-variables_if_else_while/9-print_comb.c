#include <stdio.h>
/**
 * main - Patience, persistence and perspiration make an unbeatabel
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char ch = ',';
	int i;

	for (i = '0' ; i <= '9' ; i++)
	{
	    	putchar(i);
		putcahr(ch);
		putchar('\n');
	}
	putchar('\n');
	return (0);
}
